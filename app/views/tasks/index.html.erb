<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= task.completed ? '✔️' : '⬜️'%> | <%= link_to "#{task.title}", task_path(task.id) %></li>
  <% end %>
</ul>
<hr>
<%= link_to 'Add new task', new_task_path %>
